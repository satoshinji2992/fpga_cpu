@echo off
REM==============================================
REM IVerilog安装脚本
REM==============================================echo Checking for iverilog...where iverilog >nul 2>&1if %ERRORLEVEL% EQU 0 (    echo IVerilog is already installed!    iverilog -v    goto :end)echo IVerilog not found.echo.echo Installation options:echo.echo 1. Install using Chocolatey (recommended)echo    - Run: choco install icarus-verilogecho    - Or install Chocolatey first:echo      Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))echo      choco install icarus-verilogecho.echo 2. Download from GitHub releases:echo    https://github.com/steveicarus/iverilog/releasesecho.echo 3. Use WSL (Windows Subsystem for Linux):echo    wsl --install    wsl sudo apt update    wsl sudo apt install iverilog gtkwaveecho.echo For now, use ISE ISim simulator instead.echo See doc/SIMULATION_GUIDE.md for instructions.pause:end
